# 高级前端开发工程师 — 面试题库（纯问题版）

> 候选人：王鹏伟 | 分为「简历深挖」和「高频八股文」两部分

---

## 第一部分：简历深挖（按简历原文逐条拆出）

### 一、前端基础

> 简历原文：深入掌握 HTML5 / CSS3 / JavaScript (ES6+) / TypeScript

- HTML5 新增了哪些能力？你实际用过的有哪些？Web Worker 和 Service Worker 的区别和使用场景？
- CSS3 的 `transform`、`transition`、`animation` 三者在浏览器渲染管线里的区别是什么？各自触发哪个阶段？
- ES6+ 你用得最多的特性是哪些？`Proxy` 和 `Object.defineProperty` 在拦截能力上有什么本质区别？
- TypeScript 的 `any`、`unknown`、`never` 分别用在什么场景？`declare` 和 `namespace` 你还用吗？

> 简历原文：熟悉浏览器渲染机制

- 从输入 URL 到页面渲染完成，浏览器做了什么？
- DOM 树和 CSSOM 树的构建是并行的还是串行的？有什么会阻塞 DOM 构建？
- 什么是 CRP（关键渲染路径）？怎么测量你当前页面的 CRP？
- `async` 和 `defer` 的区别？什么场景用哪个？

> 简历原文：熟悉 Event Loop 与 Promise / async/await 异步模型

- 浏览器和 Node.js 的 Event Loop 有什么本质区别？
- 看代码说输出（宏任务 / 微任务穿插题，含 Promise.then / setTimeout / async await 混合）
- `Promise.all`、`Promise.race`、`Promise.allSettled`、`Promise.any` 各自使用场景？如果有一个 reject 了，各自什么行为？
- `async/await` 怎么捕获错误？多个 await 串行 vs `Promise.all` 并行，怎么写？

> 简历原文：能针对首屏加载 / 重绘回流 / 内存泄漏做体系化性能治理

- 强制重排（Layout Thrashing）是什么？什么操作会触发？怎么避免？
- `will-change` 怎么用的？用不好有什么副作用？
- 你平时怎么检测内存泄漏？Chrome DevTools 的 Memory 面板三种视图分别看什么？
- 什么情况下会出现 Detached DOM？怎么定位到是谁持有的引用？

---

### 二、React 技术体系

> 简历原文：精通 React Hooks 组件抽象与状态管理设计

- 用 Hooks 模拟 `componentDidMount`、`componentDidUpdate`、`componentWillUnmount`，分别怎么写？哪些生命周期没法用 Hooks 模拟？
- `useEffect` 和 `useLayoutEffect` 的执行时机区别？什么场景必须要用 `useLayoutEffect`？
- `useCallback` 和 `useMemo` 的实际价值——加不加真的对性能有影响吗？什么情况下加了等于没加？
- 自定义 Hooks 怎么设计？你写过最复杂的自定义 Hook 是什么，解决了什么问题？

> 简历原文：熟练使用 React + Zustand + React Router 构建中大型 SPA/MPA

- SPA 和 MPA 的区别？你们当时怎么选择的？SPA 的 SEO 怎么解决？
- Zustand 的 `subscribe` 和 `getState` 你是怎么用的？和 React 的 `useSyncExternalStore` 有什么关系？
- Zustand 选择器（selector）的 `shallow` 比较是怎么实现的？为什么默认用 `Object.is` 而不是浅比较？
- React Router 的动态路由和嵌套路由怎么设计的？路由懒加载怎么配？

> 简历原文：结合 Ant Design Pro / Ant Design Mobile 完成中后台及移动端 H5 开发

- Ant Design Pro 的布局、权限、国际化是怎么组织的？你改过它的什么默认配置？
- Ant Design Mobile 和 Ant Design 的底层组件库有什么差异？在 H5 上用 Antd Mobile 遇到过什么坑？
- Antd 5 的 CSS-in-JS（cssinjs）相比 4.x 的 Less 方案，渲染性能有什么差异？动态主题是怎么实现的？

> 简历原文：具备大型应用的模块化拆分与复杂度管控能力

- 你们项目目录是怎么划分的？按功能分、按页面分还是混合？为什么这么分？
- 多业务团队协作时，公共代码怎么治理？你们的公共组件库 / 工具库怎么保证质量和向下兼容？
- 遇到过度抽象的情况你怎么识别和纠正？
- 你是怎么控制一个大型应用的复杂度的？具体有什么方法论或规范？

---

### 三、AI 驱动开发

> 简历原文：熟练运用 Prompt 工程、Function Calling、结构化输出等 AI 工程化手段

- Prompt Engineering 中 System Prompt、Few-shot、Chain of Thought 分别解决什么问题？你怎么测试一个 Prompt 好不好？
- Function Calling 中，如果大模型返回的工具调用的参数格式不符合预期，怎么兜底？返回了一个不存在的 tool name 怎么处理？
- 结构化输出（JSON Mode / Structured Output）和 Function Calling 的区别是什么？什么场景用哪个？

> 简历原文：落地 SSE / Stream 流式渲染完整前端方案

- SSE 和 WebSocket 各自适用场景？为什么 AI 对话场景用 SSE 而不是 WebSocket？
- 浏览器原生 `EventSource` API 的限制有哪些？为什么很多项目用 `fetch` + `ReadableStream` 替代？
- 流式渲染时，React 高频 setState 会出现什么问题？怎么优化？
- 你是怎么实现断线重连的？重连后怎么保证之前发的消息还在？

> 简历原文：基于 LangGraph 实现多轮对话上下文隔离与流程编排

- LangGraph 的 Checkpoint、StateGraph 和 Node/Edge 模型分别解决了什么问题？
- 上下文隔离具体怎么做？你是怎么判断用户切换了话题的？
- LangGraph vs LangChain 的 AgentExecutor 在流程控制上有什么区别？

> 简历原文：基于 LangChain 完成多模型数据格式化与 Function Calling 链路

- 不同大模型 API 的差异点主要在哪些地方？你是怎么做数据格式统一的？
- LangChain 的 Message 类型体系（SystemMessage / HumanMessage / AIMessage / ToolMessage）你是怎么用的？

> 简历原文：深度使用 Claude Code / AI Coding Agent，沉淀标准化 Skill/Workflow

- 你沉淀了什么类型的 Skill？具体怎么做 Prompt 设计的？
- AI 生成的代码你怎么评估质量？怎么保证生成的代码符合团队规范？
- 一个 Skill 设计好后，你怎么验证它的效果？量化指标是什么？
- 如果一个 Skill 生成的代码开始变差（因为项目规范变了），你怎么发现和更新？

---

### 四、跨端开发

> 简历原文：支付宝体系 H5、小程序开发及插件开发

- 支付宝小程序和微信小程序在 JSAPI、组件体系、分包策略上有什么差异？
- 小程序插件有什么技术限制？插件怎么做到只导出最小 API 而按需加载完整功能？
- 小程序的渲染层和逻辑层是分开的，这个架构带来了什么问题？

> 简历原文：WebView 渲染差异处理

- iOS WKWebView 和 Android WebView 的渲染差异你实际遇到了哪些？
- WKWebView 的进程模型和 Android WebView 有什么区别？对白屏兜底方案有什么影响？
- 低端 Android WebView 对 CSS 的支持有什么限制？哪些属性会触发渲染异常？

> 简历原文：JSAPI 跨端适配

- JSBridge 通信中，怎么处理 Android 和 iOS 异步回调机制的差异？
- 如果 Native 升级了 JSAPI 版本，老版本的方法被废弃了，你的 Bridge 层怎么感知和降级？
- `addJavascriptInterface`（Android）和 `WKScriptMessageHandler`（iOS）的安全差异？

> 简历原文：小程序包体积优化与资源懒加载

- 主包和分包的拆分策略是什么？公共组件和公共依赖放在哪？
- 分包预加载 `loadSubPackage` 的时机怎么控制？预加载太多有什么副作用？
- 小程序里怎么监控包体积的变化？你上线的卡口是什么样的？

---

### 五、工程化与性能优化

> 简历原文：熟悉 Webpack / Vite 工程化流程

- Webpack 和 Vite 的 HMR 实现原理有什么区别？
- Webpack 的 Loader 和 Plugin 的执行顺序和职责区别？
- Vite 的依赖预构建（Pre-Bundling）做了什么？为什么需要这一步？

> 简历原文：代码分割、懒加载、Tree Shaking

- Webpack 的 SplitChunks 是怎么配置的？`chunks: 'all'` vs `'async'` vs `'initial'` 的区别？
- Tree Shaking 的条件是什么？CommonJS 为什么不能 Tree Shaking？
- `import()` 动态导入在 Webpack 里编译后变成了什么？`/* webpackChunkName */` 等魔法注释你用过哪些？

> 简历原文：CDN 接入

- CDN 缓存策略怎么设计？HTML、JS、CSS、图片分别怎么配置缓存时间？
- 如果 CDN 挂了（比如域名不可达），前端怎么兜底？
- DNS Prefetch 和 Preconnect 的区别？在什么资源上分别使用？

> 简历原文：产物体积优化及首屏性能优化

- 产物体积分析用什么工具？你们的标准是多大算大？单文件超过多少 KB 就要拆？
- FCP、LCP、TTI、TBT、CLS 分别代表什么？怎么用 `web-vitals` 库上报 RUM 数据？
- 首屏性能优化的常见手段中，哪些是最有效的？你的优先级排序是什么？

> 简历原文：基于 Formily 设计并落地低代码表单平台，Schema 驱动

- Formily 的 Schema 联动的核心机制是什么？依赖收集是怎么做的？
- 你设计低代码表单时，怎么平衡「配置灵活性」和「逻辑表达能力」？
- Formily vs 阿里的 form-render vs 自研方案，你的选型过程？
- 低代码表单的调试体验怎么保障？出问题时怎么快速定位到根因？

---

### 六、后端能力

> 简历原文：基于 Node.js + Express/NestJS 独立开发过业务接口服务

- NestJS 的 Module / Controller / Provider 三者关系和职责？
- NestJS 的中间件（Middleware）、守卫（Guard）、拦截器（Interceptor）、管道（Pipe）的执行顺序？各自解决什么问题？
- Express 和 NestJS 的架构哲学有什么根本区别？你为什么选 NestJS 做 BFF？

> 简历原文：路由设计、中间件编写

- RESTful API 的路由设计原则？你怎么设计 URL 路径和 HTTP 方法的映射？
- 中间件的洋葱模型是什么？NestJS 怎么实现类似 Koa 的洋葱模型？

> 简历原文：MySQL / PostgreSQL + TypeORM

- TypeORM 的 Active Record 和 Data Mapper 两种模式你用的哪种？为什么？
- 你怎么管理数据库 Migration？多人开发时 Migration 冲突怎么处理？
- TypeORM 的 `relations` 和 `eager` 加载各有什么坑？N+1 查询问题怎么避免？

> 简历原文：具备 BFF 层开发能力

- BFF 层在你的架构中承担了哪些职责？为什么不能省掉这一层？
- BFF 层的鉴权怎么做？Token 验证放在哪一层？
- BFF 层的错误怎么统一处理？怎么把下游的各类错误转成前端能理解的格式？

---

### 七、工作经历深挖

> 蚂蚁集团 — 中后台前端架构与全链路交付

- 你独立完成的技术选型中，举一个最有争议的决策。当时怎么说服别人的？
- "全链路交付"具体指什么？从需求到上线你参与的所有环节？
- 同时支撑 2 个业务团队，怎么保证代码不互相阻塞？怎么处理公共依赖的版本冲突？

> 蚂蚁集团 — C 侧业务攻坚

- "关键路径技术攻坚"具体指的是什么？举一个最难的攻坚案例。
- "核心链路 P0 故障为 0"是怎么做到的？是运气还是有什么机制保障？
- 小程序和 H5 同一套业务逻辑，你怎么处理两端差异？共享代码的比例是多少？

> 蚂蚁集团 — AI 工具前端 & 效率提升 40%

- "流程配置效率提升 40%"这个数据怎么来的？你是怎么度量的？
- 你们做的 AI 工具是什么形态？嵌入到现有中后台还是独立的对话页面？
- 产品团队的流程配置痛点具体是什么？为什么 AI 能解决？

> 蚂蚁集团 — 技术共建 & 新人带教

- 你建立的 3 项前端规范具体是什么？怎么推行的？怎么保证大家遵守？
- 你说"带教 2 名新人"，具体怎么带的？怎么评估他们的成长？
- "Code Review 准入标准"是什么样的？哪些问题会被卡住不让过？

> 招银云创 — 编码规范 & Bug 率下降 30%

- ESLint / Prettier / Git Hooks 卡口是怎么接入的？怎么避免大家绕过 Hooks？
- Bug 率下降 30% 这个数据怎么计算的？是上线后发现的 Bug 还是测试阶段发现的？
- 新人上手周期从多长缩短到 2 周？具体做了什么？

> 招银云创 — 混合 H5 架构 & JSBridge

- JSBridge 协议你做了 Promise 封装，那如果一个调用链需要串行多个 Bridge 调用，你怎么处理？
- "抹平 iOS/Android API 差异"具体指哪些 API？两端的差异有多大？
- 首屏从 3s → 1.3s，这个 1.7s 分别从哪些环节省出来的？
- Crash 率是怎么统计的？Native 上报的还是前端打点？

> 招银云创 — 故障处置 & SOP

- 你主导的 2 次故障分别是什么？你当时是怎么定位到根因的？用了什么工具？
- SOP 沉淀后，你怎么验证它有效？怎么保证其他人在紧急情况下能按照 SOP 操作？

> 招银云创 — 组件库从 0 到 1

- 50+ 业务组件，你怎么划分原子组件和业务组件的边界？
- 组件设计规范具体包含什么？API 约束是什么样的？
- 组件复用率 60% 是怎么统计的？有什么组件是被多个业务方改了之后又单独维护的？

---

### 八、IOT 设备运营管理平台深挖

- 为什么选 qiankun 做微前端而不是 Module Federation？这个决策后来有没有后悔的地方？
- qiankun 的 CSS 隔离有什么局限？Ant Design 的 Modal / Dropdown 怎么穿透隔离的？
- 如果今天重新做 IOT 平台的微前端方案，你会怎么设计？
- 低代码表单引擎的 Schema 是怎么设计版本的？向前兼容怎么保证？
- 字段联动的依赖模型里，A→B→C 这种传播链路你怎么控制执行顺序？如果出现循环依赖怎么办？
- 20+ 跨业务组件的质量怎么保证？单元测试覆盖了吗？
- AntV X6 画布中，1000 个节点时怎么保证不卡？你的按需渲染方案具体怎么做？
- 画布序列化方案中，"条件分支"和"互斥分支"怎么用结构化的 JSON 表达？还原时怎么保证和保存时一模一样？
- AI Agent 的智能问答中，用户问了设备相关问题但模型答不上来，怎么兜底？
- 你的 Prompt 策略具体怎么设计的？针对"设备运营"这个领域做了哪些定制？

---

### 九、五福 NFC 项目深挖

- 为什么选 FSM（有限状态机）而不是简单的 if/else 流程？状态数量到多少时 FSM 的优势才开始显现？
- 你的 FSM 是怎么保证"非法状态跃迁"在编译期就能被拦截的？
- 广播同步机制中，如果 A 碰撞成功但广播到 B 的时候 B 网络超时了，B 状态怎么恢复？
- 计时器按时间窗口下发引导提示，你是怎么设计时间窗口和提示文案的对应关系的？
- 设备性能分级的评分公式是什么？各因素（CPU 核心数、内存、GPU 型号）的权重是多少？
- 分位线划在哪（高分位 / 中分位 / 低分位）？为什么这么划？
- "降级决策在首帧渲染前完成"——你是怎么在渲染前就拿到设备信息的？
- CSS/SVG 动画替代 Mars 引擎的 Canvas 动画，效果上妥协了什么？用户能感知到差异吗？
- FCP 从 4.5s → 2.0s，具体每一项（DNS、HTML、JS、图片）分别优化了多少？怎么验证的？
- 资源预加载器的设计策略是什么？怎么判断下一个阶段要预加载哪些资源？
- 实时性能监控是怎么做的？上报了哪些指标？异常告警的阈值是怎么定的？
- 活动 14 天 0 客诉是怎么做到的？有什么机制保障的？

---

### 十、技术决策与软技能

- 过去 5 年里，你做过的最失败的一个技术决策是什么？后来怎么纠正的？
- 你选技术方案时，最看重的 3 个因素是什么？为什么是这 3 个？
- 如果一个产品和你说"这个需求很简单，为什么需要 3 天"，你会怎么回应？
- 你怎么保持自己的技术更新？最近半年你关注的前沿技术或趋势是什么？
- 你 GitHub 上 `AI` 这个项目是做什么的？是你个人学习项目还是实际在用的工具？

---

## 第二部分：高频八股文（JS / TS / CSS / 浏览器 / 网络 / 安全）

### JS 基础

1. 原型链：`__proto__` 和 `prototype` 的关系？`Object.create(null)` 创建的对象有什么特殊？
2. 继承：ES5 寄生组合式继承和 ES6 `class extends` 的底层区别？`super()` 做了什么？
3. 闭包：闭包的经典应用场景有哪些？什么情况下闭包会导致内存泄漏？怎么用 Chrome DevTools 定位？
4. this：箭头函数和普通函数中 `this` 的区别？`call` / `apply` / `bind` 的底层实现？箭头函数的 `this` 能改吗？
5. new：`new` 操作符做了什么？如果构造函数显式 `return` 一个对象 / 基本类型，`new` 返回什么？
6. 防抖与节流：防抖（debounce）和节流（throttle）的区别？各自的适用场景？如何实现一个带立即执行选项的防抖？
7. 深拷贝：写出一个能处理 `Date`、`RegExp`、`Map`、`Set`、循环引用的深拷贝。`structuredClone` 的局限是什么？
8. 柯里化：柯里化在你实际项目中有用过吗？偏函数和柯里化的区别？
9. 模块化：ESM 和 CommonJS 的本质区别？`import` 的静态分析和动态 `import()` 分别用在什么场景？Tree Shaking 为什么只能基于 ESM？
10. WeakMap / WeakSet：和 Map / Set 的区别？为什么它们存储的对象可以被 GC 回收？WeakMap 的实际应用场景？
11. 迭代器 / Generator：`Symbol.iterator` 和 `for...of` 的关系？Generator 在异步流程控制中的应用（co 库原理）？
12. Reflect / Proxy：Reflect 的设计动机？为什么要有 `Reflect.get` 而不直接用 `target[key]`？

### JS 异步

13. Promise 手写：手写一个符合 Promise/A+ 规范的 Promise（含 then 链式调用、状态不可逆、异步执行）
14. Promise 静态方法：手写 `Promise.all`、`Promise.race`、`Promise.allSettled`、`Promise.any`
15. 错误捕获：`try/catch` 能捕获 `setTimeout` 里的错误吗？Promise 的 reject 怎么在全局兜底？`unhandledrejection` 事件怎么用？
16. async/await 原理：`async` 函数返回什么？`await` 后面的代码相当于 `Promise.then` 里的什么？怎么用 Generator + Promise 模拟 async/await？
17. 并发控制：实现一个函数，限制同时最多 N 个异步任务，且按传入顺序输出结果
18. 红绿灯：用 JS 实现一个红绿灯交替亮（Promise + setTimeout / async/await）
19. 中断：`AbortController` 和 `AbortSignal` 的用法？怎么中断一个 fetch 请求？怎么让自定义异步任务支持中断？

### TypeScript

20. 类型运算：`Partial`、`Required`、`Readonly`、`Pick`、`Omit`、`Record`、`Exclude`、`Extract`、`NonNullable`、`ReturnType`、`Parameters` 怎么实现的？
21. 条件类型：`T extends U ? X : Y` 在哪些场景用？分布式条件类型的触发条件是什么？怎么避免？
22. infer：`infer` 关键字怎么用？怎么提取 Promise 包裹的类型（`Awaited`）？
23. 协变与逆变：函数参数是逆变的，返回值是协变的——什么意思？什么时候会遇到？
24. 类型兼容：结构化类型（Structural Typing）和名义化类型（Nominal Typing）的区别？TS 中怎么模拟 Nominal Typing？
25. declare：`declare` 什么时候用？和 `export` / `import` 的关系？声明文件怎么写？
26. const assertion：`as const` 做了什么？和 `Object.freeze` 的区别？
27. satisfies：TS 4.9 的 `satisfies` 解决了什么问题？和类型标注有什么区别？
28. enum：TS enum 的问题？为什么社区倾向于用字符串联合类型替代 enum？

### CSS

29. BFC：块级格式化上下文（BFC）的形成条件和作用？怎么清除浮动？`overflow: hidden` 为什么能清除浮动？
30. 盒模型：标准盒模型和 IE 盒模型的区别？`box-sizing: border-box` 更适合实际开发的原因？
31. 层叠上下文：什么是层叠上下文（Stacking Context）？哪些属性会创建新的层叠上下文？`z-index` 为什么不生效？
32. 垂直居中：至少 5 种实现元素水平垂直居中的方式，以及各自的适用场景和局限
33. 三栏布局：左右固定宽度，中间自适应的三栏布局，有哪几种实现方式？
34. 响应式：`rem` / `em` / `vw` / `vh` / `%` 的各自计算基准是什么？移动端 1px 边框问题怎么解决？
35. Grid vs Flex：CSS Grid 和 Flexbox 的定位差异？什么布局用 Grid 更合适？
36. 包含块（Containing Block）：什么是包含块？哪些元素会改变包含块的计算规则？
37. 伪元素与伪类：`::before` / `::after` 和 `:hover` / `:focus` 的本质区别？为什么一个用双冒号一个用单冒号？
38. 动画性能：`transform` 和 `left/top` 做位移动画，性能差异的本质原因是什么？`will-change` 的副作用？`FLIP` 动画技术？

### 浏览器

39. 事件循环：宏任务 / 微任务分类？UI Rendering 在事件循环中的位置？`requestAnimationFrame` 和微任务谁先执行？
40. 缓存策略：强缓存和协商缓存的区别？`Cache-Control` 的 `no-cache` 和 `no-store` 分别什么意思？`max-age=0` 呢？
41. Cookie：Cookie 的 `Domain`、`Path`、`HttpOnly`、`Secure`、`SameSite` 分别控制什么？SameSite 的三个值和 CSRF 的关系？
42. 本地存储：Cookie、localStorage、sessionStorage、IndexedDB 的对比（容量、是否随请求发送、生命周期、异步同步）
43. 跨域：跨域的判定标准（协议、域名、端口全一样）？CORS 的简单请求和预检请求的触发条件？JSONP 的缺陷？
44. 进程：浏览器多进程架构（主进程、渲染进程、GPU 进程、网络进程、插件进程）各自负责什么？为什么一个 tab 崩溃不影响其他 tab？
45. 线程：渲染进程中的 GUI 渲染线程和 JS 引擎线程为什么互斥？Worker 可以操作 DOM 吗？
46. V8 GC：V8 的分代垃圾回收策略？新生代（Scavenge）和老生代（Mark-Sweep-Compact）各自怎么工作？增量标记是什么？
47. 重排重绘合成：Layout / Paint / Composite 三阶段分别做了什么？只触发 Composite 的 CSS 属性有哪些？
48. 合成层：什么情况下浏览器会创建合成层？隐式合成（implicit compositing）是什么？Layers 面板怎么看？

### 网络

49. HTTP 版本：HTTP/1.0 vs 1.1 vs 2.0 vs 3.0 的核心变化？HTTP/2 多路复用解决了什么问题又引入了什么新问题（队头阻塞）？
50. HTTPS：HTTPS 握手过程？对称加密和非对称加密各自用在哪一步？证书链是怎么验证的？
51. TCP 三次握手四次挥手：为什么是三次不是两次？为什么第四次挥手后客户端还要等 2MSL？TCP 队头阻塞是什么？
52. CDN 原理：CDN 如何选择最优节点？DNS 调度和 Anycast 调度的区别？CDN 的回源机制？
53. 状态码：301 vs 302 vs 307 vs 308 区别？401 vs 403 区别？502 vs 504 区别？
54. 请求头：`Connection: keep-alive` vs `Upgrade`、`Content-Encoding` vs `Transfer-Encoding`、`ETag` vs `Last-Modified`

### 安全

55. XSS：存储型、反射型、DOM 型 XSS 分别怎么攻击？CSP 能防护到哪种？`innerHTML` 和 `document.write` 的危险？
56. CSRF：CSRF 攻击原理和防护手段？SameSite Cookie 能完全替代 CSRF Token 吗？`Origin` / `Referer` 校验靠不靠谱？
57. CSP：内容安全策略怎么配？`nonce` 和 `hash` 的工作方式？`report-uri` / `report-to` 怎么用？
58. 点击劫持：怎么做防护？`X-Frame-Options` 和 `frame-ancestors` 的区别？
59. CORS 安全：`Access-Control-Allow-Origin: *` 什么时候不能用？`Access-Control-Allow-Credentials: true` 的注意事项？
60. SRI：Subresource Integrity 是什么？怎么保障 CDN 资源的完整性？

### 综合

61. 前端监控：如果你要设计一个前端错误监控 SDK，你会收集哪些信息？怎么保证 SDK 自己不拖慢页面？sourcemap 怎么看？
62. 微前端原理：你会怎么实现一个简易的 JS 沙箱？Proxy 方案怎么防止逃逸？
63. 设计模式：你在项目中主动使用过哪些设计模式？观察者模式、发布订阅模式、策略模式在前端的具体应用？
64. 组件设计：你心目中一个好的 UI 组件应该满足哪些原则？你觉得 Ant Design 的组件设计好在哪里、不好在哪里？

---

> 共计 **150+ 题**，覆盖简历上所有技术关键词和项目细节，外加前端高频八股文。建议面试时按岗位侧重点选 15~20 题作为核心面试题，其余作为追问储备。
